export default function getStyle(data) {
    return {
        container: {
            width: data.width,
            height: data.height,
            borderRadius: 12,
            paddingLeft: 0,
            paddingRight: 0,
        },
        rankList: {
            width: Math.ceil(data.width),
            height: data.height,
        },
        list: {
            width: Math.ceil(data.width),
            height: data.height,
        },
        listItem: {
            position: 'relative',
            width: Math.ceil(data.width),
            height: data.height / 10,
            flexDirection: 'row',
            alignItems: 'center',
            marginTop: 2,
        },
        rankBg: {
            position: 'absolute',
            top: 0,
            left: 0,
            width: Math.ceil(data.width),
            height: data.height / 10,
        },
        rankAvatarBg: {
            position: 'absolute',
            top: data.height / 10 * 0.1,
            left: data.width * 0.08,
            width: data.height / 10 * 0.8,
            height: data.height / 10 * 0.8,
        },
        rankAvatar: {
            borderRadius: 0,
            marginLeft: data.width * 0.08,
            width: data.height / 10 * 0.8,
            height: data.height / 10 * 0.8,
        },
        rankNameView: {
            position: 'relative',
            marginLeft: data.width * 0.05,
            width: data.width * 0.35,
            height: data.height / 10,
        },
        rankName: {
            position: 'absolute',
            top: 0,
            left: 0,
            width: data.width * 0.35,
            height: data.height / 10,
            textAlign: 'center',
            lineHeight: data.height / 10,
            fontSize: data.height * 0.03,
            textOverflow: 'ellipsis',
            color: '#fff',
        },
        rankScoreView: {
            position: 'relative',
            marginLeft: data.width * 0.05,
            width: data.width * 0.2,
            height: data.height / 10,
        },
        rankScoreVal: {
            position: 'absolute',
            top: 0,
            left: 0,
            width: data.width * 0.2,
            height: data.height / 10,
            textAlign: 'center',
            lineHeight: data.height / 10,
            fontSize: data.height * 0.03,
            color: '#fff',
        },
        shareToBtn: {
            position: 'relative',
            marginLeft: data.width * 0.05,
            width: data.height / 10 * 0.6,
            height: data.height / 10 * 0.6,
        },
        shareBtnBg: {
            position: 'absolute',
            top: 0,
            right: 0,
            width: data.height / 10 * 0.6,
            height: data.height / 10 * 0.6,
        },
    };
}
