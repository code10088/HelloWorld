using System;
public class NetMsgId
{
{{~ for msg in messages ~}}
    public const ushort {{ msg.name }} = {{ msg.id }};
{{~ end ~}}
}
public partial class NetMsgDispatch
{
    private bool Deserialize(ushort id, UnsafeByteBuffer buffer)
    {
        try
        {
            IDeserialize msg = null;
            switch (id)
            {
{{~ for msg in messages ~}}
    {{~ if msg.id >= 10000 ~}}
                case NetMsgId.{{ msg.name }}: msg = new {{ msg.type }}(); break;
    {{~ end ~}}
{{~ end ~}}
            }
            msg.Deserialize(buffer);
            HandleMsg(id, msg);
            return true;
        }
        catch (Exception e)
        {
            GameDebug.LogError(e.Message);
            return false;
        }
    }
}